name: Release

# Triggers on semver tags only (1.0.0, 1.2.3, 2.0.0-rc1). An optional
# leading `v` is accepted (v1.0.0). Non-numeric tags are ignored.
# Mirrors darshana's release trigger surface.
on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
      - '[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: write

jobs:
  # ────────────────────────────────────────────────────────────────
  # Gate — full CI must pass before anything builds for release.
  # Reuses ci.yml via workflow_call so a release can never ship a
  # binary that hasn't passed lint / vet / tests / the full smoke
  # suite / DCE parity / security scan / docs + version-consistency.
  # ────────────────────────────────────────────────────────────────
  ci:
    name: CI gate
    uses: ./.github/workflows/ci.yml

  # ────────────────────────────────────────────────────────────────
  # Verify — VERSION file, cyrius.cyml indirection, the baked source
  # literal, and the git tag must all agree before anything is
  # packaged. Unlike darshana (whose dist header is generated from
  # VERSION), darshini carries a hand-bumped _darshini_version_str()
  # literal, so we assert it too. Exposes `version` for downstream.
  # ────────────────────────────────────────────────────────────────
  version-verify:
    name: Version verify
    needs: ci
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.extract.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - name: Extract VERSION / tag / cyml / source literal
        id: extract
        run: |
          TAG="${GITHUB_REF_NAME#v}"   # strip optional leading 'v'
          echo "$TAG" | grep -qE '^[0-9]+\.[0-9]+\.[0-9]+' \
            || { echo "::error::tag ($TAG) is not semver"; exit 1; }

          VERSION=$(cat VERSION | tr -d '[:space:]')
          echo "VERSION file:  $VERSION"
          echo "git tag:       $TAG"

          if ! grep -qE '^version *= *"\$\{file:VERSION\}"' cyrius.cyml; then
            echo "::error::cyrius.cyml version no longer resolves via \${file:VERSION}" >&2
            exit 1
          fi

          if ! grep -qE "return \"darshini ${VERSION}\";" src/main.cyr; then
            echo "::error::_darshini_version_str() not bumped to ${VERSION}" >&2
            exit 1
          fi

          if ! grep -qE "^## \[${VERSION}\]" CHANGELOG.md; then
            echo "::error::CHANGELOG.md missing section [${VERSION}]" >&2
            exit 1
          fi

          if [ "$VERSION" != "$TAG" ]; then
            echo "::error::VERSION ($VERSION) != git tag ($TAG)" >&2
            exit 1
          fi

          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

  # ────────────────────────────────────────────────────────────────
  # Package — build the release binary and bundle it with project
  # metadata. darshini's consumable is the `darshini` binary (NOT a
  # dist .cyr like darshana). Release builds run the DCE pass — the
  # CI gate already proved DCE parity, so the shipped bytes are the
  # eliminated build.
  # ────────────────────────────────────────────────────────────────
  package:
    name: Package
    needs: version-verify
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ needs.version-verify.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Cyrius toolchain
        # The cyrius.cyml `[package].cyrius` pin is the single source of truth
        # for the toolchain version (CLAUDE.md "no hardcoded toolchain versions
        # in CI YAML") — read the pin and hand it to the upstream installer.
        #
        # Do NOT hand-roll this by untarring the release asset into
        # $HOME/.cyrius/{bin,lib}: that is the pre-6.5 layout, and it is what
        # this step replaced at v1.3.1. From 6.5.x `cyrius deps` resolves the
        # stdlib snapshot from $HOME/.cyrius/versions/$CYRIUS_VERSION/lib and
        # hard-fails with "pins version X but it is not installed" if only the
        # flattened dirs exist. The installer lays out versions/<v>/{bin,lib}
        # and symlinks bin/ + lib/ at it, and additionally does SHA256 +
        # signature verification the hand-rolled block skipped. Same pattern
        # as darshana / patra / libro.
        run: |
          set -e
          CYRIUS_VERSION="$(grep '^cyrius = ' cyrius.cyml | head -1 | sed 's/cyrius = "\(.*\)"/\1/')"
          echo "Installing Cyrius $CYRIUS_VERSION"
          curl -sSf https://raw.githubusercontent.com/MacCracken/cyrius/main/scripts/install.sh | \
            CYRIUS_VERSION="$CYRIUS_VERSION" sh
          echo "$HOME/.cyrius/bin" >> "$GITHUB_PATH"

      - name: Verify toolchain layout
        # Guards the exact failure this replaced: a successful-looking install
        # that leaves no versions/<v>/lib snapshot, so every later cyrius
        # invocation dies on dep resolution instead of here.
        run: |
          set -e
          CYRIUS_VERSION="$(grep '^cyrius = ' cyrius.cyml | head -1 | sed 's/cyrius = "\(.*\)"/\1/')"
          test -d "$HOME/.cyrius/versions/$CYRIUS_VERSION/lib" \
            || { echo "::error::stdlib snapshot missing at ~/.cyrius/versions/$CYRIUS_VERSION/lib"; exit 1; }
          cyrius version
          echo "toolchain ok: pin $CYRIUS_VERSION, snapshot present"

      - name: Resolve dependencies
        run: cyrius deps

      - name: Build release binary (DCE)
        run: |
          mkdir -p build
          CYRIUS_DCE=1 cyrius build src/main.cyr "build/${{ github.event.repository.name }}"
          echo "build/${{ github.event.repository.name }}: $(wc -c < build/${{ github.event.repository.name }}) bytes"

      - name: Package binary artifact
        # Tarball (binary + minimal metadata) for offline mirrors /
        # vendoring, plus the standalone binary for direct download.
        run: |
          BIN="${{ github.event.repository.name }}"
          PKG="${BIN}-${VERSION}-x86_64-linux"
          mkdir -p "pkg/$PKG"
          cp "build/$BIN" "pkg/$PKG/"
          cp README.md LICENSE CHANGELOG.md VERSION cyrius.cyml "pkg/$PKG/"
          mkdir -p out
          tar czf "out/${PKG}.tar.gz" -C pkg "$PKG"
          # Standalone binary for direct fetch.
          cp "build/$BIN" "out/${BIN}-${VERSION}-x86_64-linux"

      - name: Generate SHA256
        run: |
          cd out
          sha256sum * > SHA256SUMS.dist

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-dist
          path: |
            out/*.tar.gz
            out/${{ github.event.repository.name }}-*-x86_64-linux
            out/SHA256SUMS.*

  # ────────────────────────────────────────────────────────────────
  # Source tarball — language-agnostic, always linux runner.
  # ────────────────────────────────────────────────────────────────
  source:
    name: Source tarball
    needs: version-verify
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ needs.version-verify.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - name: Create source tarball
        run: |
          mkdir -p out
          git archive --format=tar.gz \
            --prefix="${{ github.event.repository.name }}-${VERSION}/" \
            -o "out/${{ github.event.repository.name }}-${VERSION}-src.tar.gz" HEAD
          cd out
          sha256sum "${{ github.event.repository.name }}-${VERSION}-src.tar.gz" > SHA256SUMS.src

      - name: Upload source artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-source
          path: |
            out/*.tar.gz
            out/SHA256SUMS.src

  # ────────────────────────────────────────────────────────────────
  # Release — collect artifacts + checksums, publish GitHub release.
  # Body pulled from the matching CHANGELOG.md section. Pre-1.0
  # releases publish as prerelease so package managers don't
  # auto-promote them.
  # ────────────────────────────────────────────────────────────────
  release:
    name: GitHub release
    needs: [package, source, version-verify]
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ needs.version-verify.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - name: Download all release artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist-raw

      - name: Consolidate artifacts
        run: |
          mkdir -p out
          find dist-raw -type f \
            \( -name '*.tar.gz' -o -name '${{ github.event.repository.name }}-*-x86_64-linux' -o -name 'SHA256SUMS.*' \) \
            -exec cp {} out/ \;
          cd out
          cat SHA256SUMS.* | sort -u > SHA256SUMS
          rm -f SHA256SUMS.*
          ls -la

      - name: Extract release body from CHANGELOG
        run: |
          awk -v ver="${VERSION}" '
            $0 ~ "^## \\[" ver "\\]" { found=1; next }
            found && /^## \[/         { exit }
            found                    { print }
          ' CHANGELOG.md > /tmp/body.md
          if [ ! -s /tmp/body.md ]; then
            echo "::warning::CHANGELOG.md has no body for ${VERSION}; falling back to generated notes"
          fi

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          name: "${{ github.event.repository.name }} ${{ env.VERSION }}"
          body_path: /tmp/body.md
          prerelease: ${{ startsWith(env.VERSION, '0.') }}
          files: |
            out/*.tar.gz
            out/${{ github.event.repository.name }}-*-x86_64-linux
            out/SHA256SUMS
          generate_release_notes: true
          fail_on_unmatched_files: true
